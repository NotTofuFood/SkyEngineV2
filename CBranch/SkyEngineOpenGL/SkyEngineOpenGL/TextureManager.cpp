#include "TextureManager.h"

#define STB_IMAGE_IMPLEMENTATION
#include "stb_image.h"

TextureManager::TextureManager()
{
}

unsigned char* TextureManager::initTexture(char* name, unsigned int type) {
	//unsigned char *texture = stbi_load(name, &texture_width, &texture_height, &texture_channels, 0);

	return 0;
}

void TextureManager::useTexture(unsigned char* id) {


}
